apiVersion: v1
kind: Service
metadata:
  name: subscription-platform
  labels:
    app: subscription-platform
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: subscription-platform