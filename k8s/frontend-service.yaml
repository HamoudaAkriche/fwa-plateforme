apiVersion: v1
kind: Service
metadata:
  name: fwa-subscription-ui
  labels:
    app: fwa-subscription-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: fwa-subscription-ui
